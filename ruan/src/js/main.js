// const header = require('');  //引入公共页面头
const topbar = require('../html/smart.html'); //引入body中存在的公共模块
// const footer = require('');  //引入公页面脚
const htmlconfig = {
    // header: header,
    topbar: topbar,
    //footer: footer
};
module.exports = htmlconfig; //导出供调用